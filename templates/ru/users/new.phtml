<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
  <title>Регистрация</title>
</head>
<body>
  <div class="container mt-3">
    <div class="row">
      <div class="col-sm-5 col-12 mx-auto mt-4">
        <div class="card">
          <div class="card-body">
            <h5 class="card-title mb-4">Регистрация</h5>
            <form action="/users" method="post" enctype="multipart/form-data" data-form="sign-up">
              <div class="form-group">
                <label for="InputLogin">Логин*</label>
                <input type="text" class="form-control" id="InputLogin" name="login">
                <?= $alreadyTaken ?? null?>
                <?= $loginError ?? null?>
              </div>
              <div class="form-group">
                <label for="InputEmail1">Email*</label>
                <input type="email" class="form-control" id="InputEmail1" name="email">
                <?= $emailError ?? null ?>
              </div>
              <div class="form-group">
                <label for="InputPhone">Телефон</label>
                <input type="tel" name="phone" id="InputPhone" class="form-control bfh-phone"/>
                <? $phoneError ?? null ?>
              </div>
              <div class="form-group">
                <label for="InputPassword1">Пароль*</label>
                <input type="password" class="form-control" id="InputPassword1" name="password">
              </div>
              <? $passwordError ?? null ?>
              <div class="form-group">
                <label for="InputPassword2">Повторите пароль*</label>
                <input type="password" class="form-control" id="InputPassword2" name="password_confirm">
              </div>
              <? $confirmError ?? null ?>
              <div class="form-group">
              <label for="customFileLangHTML">Прикрепить файл:</label>
              <div class="custom-file">
                  <input type="file" class="custom-file-input <?= $fileError ? ' is-invalid' : null?>" id="customFileLangHTML" name="image_file">
                  <?= $fileError ? "<div class='invalid-feedback mb-3'>{$fileError}</div>" : null ?>
                  <label class="custom-file-label" for="customFileLangHTML" data-browse="File"><small class="form-text text-muted">Поддерживаемые форматы: png, jpeg, gif</small></label>
                </div>
                <?= $fileError ? null : '<small class="form-text text-muted">Максимальный размер файла - 10Мб.</small>' ?>
              </div>
              <button type="submit" class="btn btn-primary col-12 mt-2" id='submitButton' disabled>Зарегестрироваться</button>
            </form>
          </div>
        </div>
        <div class="card mt-3">
          <div class="card-body text-center">
             <span>Уже есть аккаунт? <a href="/">Войти</a></span>
          </div>
        </div>
        <form action="/en" method="post">
          <button type="submit" class="btn col-3 btn-outline-info mt-3">English</button>
        </form>
      </div>
    </div>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/bs-custom-file-input/dist/bs-custom-file-input.min.js"></script>
  <script src="/scripts/main.js"></script>
</body>
</html>